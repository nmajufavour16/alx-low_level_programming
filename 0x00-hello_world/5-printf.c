#include <stdio.h>

/*
 * main - Using the printf command to print a line
 *
 * return: always 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
